# APCS9 Robust Input Practice

Canonical source repository: `APCS`

This project consolidates the legacy `APCS9-Robust-Input-Practice` and `APCS9-Robust-Input-Practice-Starter` folders into the common course
wrapper layout.

## Structure

- `starter/` contains the starter/template snapshot.
- `solution/` contains the solution/reference snapshot.
