# APCS1 Mad Libs

Canonical source repository: `APCS`

This project consolidates the legacy `APCS1-Mad-Libs` and `APCS1-Mad-Libs-Template` folders into the common course wrapper layout.

## Structure

- `starter/` contains the starter/template snapshot.
- `solution/` contains the solution/reference snapshot.
