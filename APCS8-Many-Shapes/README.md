# APCS8 Many Shapes

Canonical source repository: `APCS`

This project consolidates the legacy `APCS8-Many-Shapes` and `APCS8-Many-Shapes-Starter` folders into the common course wrapper layout.

## Structure

- `starter/` contains the starter/template snapshot.
- `solution/` contains the solution/reference snapshot.
